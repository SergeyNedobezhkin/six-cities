export enum AppRoute {
  Root = '/',
  Login = '/login',
  Favorites = '/favorites',
  Offer = '/offer',
}


export enum AuthorizationStatus {
  NoAuth = "NO_AUTH",
  Auth = "AUTH",
  Unknown = "UNKNOWN",
}
